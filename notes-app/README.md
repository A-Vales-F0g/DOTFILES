# Notes App

A simple, single-file notes app that saves your notes in the browser using `localStorage`.

## How to use

Open `index.html` in any modern web browser. Type a note and click **Add** or press `Enter`. Click **Delete** to remove a note. Notes persist between sessions in your browser's local storage.

## Files

- `index.html` — complete app with markup, styles, and JavaScript
